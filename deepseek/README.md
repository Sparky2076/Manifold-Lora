# DeepSeek SFT（占位）

此前 **`deepseek/`** 下旧版 SFT 脚本与实验记录已移除，便于后续重写 **DeepSeek 网格搜索** 与流水线。

新代码将仍放在本目录，并复用仓库根的 `lora.py`、`mlora.py`、`optimizers.py`。设计说明可参考 [`docs/DEEPSEEK_FINETUNE_PLAN.md`](../docs/DEEPSEEK_FINETUNE_PLAN.md)（与实现可能不完全一致，以新代码为准）。
